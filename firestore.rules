rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas permisivas: solo requieren autenticación
    // La seguridad se implementa en la aplicación filtrando por userId en las consultas
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 